# streamlit_app

Streamlit app to deploy a scavenger hunt

* `scavenger_hunt.py`

* Installation
 ```bash
    pip install -r requirements.txt
  ```

* Run

  ```bash
  streamlit run scavenger_hunt.py
  ```
